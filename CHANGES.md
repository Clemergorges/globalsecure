# CHANGES (Identity Flow Fixes)

## Unreleased

- fix(kyc): Stripe Identity com validação de env, return_url robusto e auditoria
- fix(kyc): UX de verificação automática com loader, hint HTTPS mobile e fallback para upload manual
- feat(security): fluxo completo de OTP por e-mail para troca de senha (UI + códigos padronizados)
- fix(security): sessões com isCurrent correto e metadados (device/location/lastActive)
- chore(i18n): novas chaves em pt/en/fr/de + suporte parcial a es via fallback por merge
- chore(env): documentação de STRIPE_SECRET_KEY, NEXT_PUBLIC_APP_URL, BLOB_READ_WRITE_TOKEN e SMTP

